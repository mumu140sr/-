# このリポジトリで作業するときの注意

## JS/CSS を変えたら、必ずバージョン番号を上げる

このアプリは GitHub Pages で配信しており、ブラウザのキャッシュを避けるために
`?v=NNN` を付けて読み込んでいる。**この番号を上げ忘れると、ファイルを直して
push しても利用者の画面はまったく変わらない。**

番号は次の5ファイルに散っているので、まとめて置換すること。

```
index.html            <script src="js/xxx.v6.js?v=NNN">  / css も
js/milp.worker.js     self.importScripts('...?v=NNN')
js/milp.v6.js
js/worker-client.v6.js
js/optimizer.worker.js
```

```bash
# 例: 205 → 206
grep -rl "v=205" --include=*.html --include=*.js . | xargs sed -i 's/v=205/v=206/g'
```

画面右上の `#appVersion`（index.html）にも同じ番号を書く。ここを見れば、
利用者の画面がどのバージョンを読み込んでいるか確認できる。

## 対になっているファイルは中身を完全に一致させる

ページ本体とワーカーで別名の同じファイルを読んでいる。**片方だけ直すと、
画面の検査結果と生成結果が食い違う。**

| ページが読む | ワーカーが読む |
|---|---|
| `js/data.v6.js` | `js/data.js` |
| `js/optimizer.v6.js` | `js/optimizer.js` |

```bash
cmp js/data.js js/data.v6.js && cmp js/optimizer.js js/optimizer.v6.js
```

`js/milp-core.js` と `js/milp.worker.js` はワーカー専用で、対になる `.v6` は無い。

## HTML の構造を変えたら、タグの数を数える

`<div>` を `<details>` に替えたときに閉じタグが余り、画面が崩れたことがある。

```bash
python3 -c "s=open('index.html',encoding='utf-8').read(); print(s.count('<div'), s.count('</div>'))"
```

## ルールの追加・重み変更は、実データで測ってから入れる

見た目の理屈で良さそうな変更が、実測では悪化することが多い。
`checkViolations` の件数で前後を比べてから採用する。過去に、
かたまり最低日数の制約と段の順番入れ替えを理屈で入れて、
どちらも実測で悪化したため取り消している。

利用者に提案する数値は、必ず実際に生成・検査して得た値を使う。

## 件数だけで良し悪しを判断しない

エラーには 🚨（連勤超過・人員不足など必ず直すもの）と 🟡（できれば避けたい
もの）があり、重さがまったく違う。**合計件数だけを見ると、🟡が2件減って
🚨が1件増えた変更を「1件減った」と誤判定する。**

過去に2回これで失敗している。

- 「遅→休→早を気にしない」個人設定: 合計 41件 → 14件 と大きく減ったが、
  エラーを消したのではなく数えなくしただけで、別のルールが倍増していた。
- 入れ替えによる修正案: 合計は減るが 🚨 連勤超過を新たに発生させた。

判定するときは必ず次の2つを分けて数え、**🚨 が増える案は採用しない**。

```js
const isMust = (t) => getRuleLevel(t) === 'must' || MUST_TYPES_OPT.has(t);
```

利用者に見せる画面にも、🚨 が増えないことが分かるように出す。

## 6連勤以上はコンプライアンス違反（⛔）

- **6連勤以上は、連勤の上限の設定にかかわらず違反**（5連勤までは可）。
  前月末からの続きと、半休の日も出勤として数える（`COMPLIANCE_CONS_DAYS = 6`）。
- 上限4の人の5連勤は、ふつうの 🚨 連勤超過（⛔ではない）。
- ⛔ は画面のどこでも 🚨 と分けて出す（報告・修正案・余の変更・手直しの警告）。
- 生成は最初の段で「どの6日間も出勤5日以下」を解く（milp-core のコンプラの段）。
  固定セルが無くても、この段の答えを捨てない（allowEmpty）。
- 自動の処理（生成・仕上げ・修正案・余の変更）は、⛔ を新しく作らない・長く
  しない・つなげない（`compWorsened`）。上限を6以上に設定すると画面で警告する。
- 手で直して ⛔ になったときは、止めずに警告を出す。

## 比べ方と並べ方（2つを分けて使う）

**改善かどうか（scoreBetter）**: 次をすべて満たし、どれかが減ったときだけ改善。
🚨 を種類をまたいで取り替えない（例: 人員不足1件を消して連勤超過1件を作る案は採らない）。

- 🚨 のどの種類の件数も増えない
- 連勤の超過日数が増えない
- ⛔ の件数が増えず、⛔ が新しく・長く・つながっていない
- 🟡 は 🚨 がすべて同じときだけ比べる

**どれが良いか並べる（scoreCompare）**: 4通りから選ぶ・修正案を並べるとき。

1. ⛔ 6連勤以上の件数
2. 人員不足
3. 🚨 の合計（連勤は1続きで1件。6連勤以上は ①と③の両方に数える）
4. 連勤の超過日数
5. 🟡 の件数

③を④より先にしたのは利用者の判断（2026年9月24日）。10月の表の版違い（oct）で、
「5連勤1回」と「単発出勤1件＋責任者の順位1件」を比べ、5連勤1回のほうが
良いとした。ほかの月ではまだ確かめていない。

## 「遅番を分け合う」と「切替を減らす」は逆を向いている

遅番が1人に偏る（実データで月22日中21日という人がいた）のは、
「早遅バランス＝指定なし」の人に遅番回数の歯止めが無いため。ただし、
偏りを直そうとすると切替を減らす仕組みと正面からぶつかる。
**切替を減らす＝その人を1か月同じ時間帯に固定する**、だからである。
そして切替を減らす仕組みは「遅→休→早」を減らすために入れたもの。

試して取り消した案:

- **最小最大（いちばん遅番が多い人の上限を下げる）**: 重み60で下の段に
  置いた版は、上の段で切替が予算として固定済みのため動く余地がなく無力
  （oct 差12→17・🚨3→4、oct11 差15→15・🚨2→3）。切替と同じ段へ移し
  重み2000にしても、遅番1日ぶんの得(800点)より切替1回の損(9000点)が
  大きく、「1人に寄せたまま切替を減らす」ほうが得のまま
  （oct 合計12→14・🚨3→4・差12→16）。重みで勝たせれば切替が壊れる。
- **全員を「均等」にする**: 月による。oct は遅休早 7→5件・1人最多2→1件で
  🚨据え置きと良かったが、oct13 は横ばい、**oct16 は 🚨 2→6 と悪化**。
  遅番のスキル保有者が少ない月に均等を強いると人員が回らなくなるため。

「早遅バランス」と「早遅の切り替え（band-switch）」を両方「絶対」にすると、
先に解いたほうが0になり、後のほうが崩れる（oct11、3回とも同じ: 切り替えを
先にすると 🚨3・バランスのずれ最大10日、バランスを先にすると 🚨4・切り替え
最大4回で4人が上限超え）。利用者の判断（2026年9月24日）で、**両方を「絶対」に
しない使い方**にし、どちらを先に解くかは決めない（今の並びのまま）。設定画面で
両方を「絶対」にすると、どちらかを「できれば」にするよう知らせる。

### 早遅の切り替えの検査（v220、2026年9月24日）

- 目的は**見えるようにすること**。切り替えが月の上限（既定2回）を超えた人を、
  どの日に切り替えたかと一緒に出す（'band-switch'）。数え方は生成の計算と同じ
  （休みの日は直前の時間帯を引き継ぐ、半休は早番扱い）。
- 「途中から作り直す」では、連勤と同じく**最後に切り替えた日**で前半か後半かを決める。
- 比べ方: 「絶対」にしたときだけ、並べ方の④（連勤の超過日数）の次に
  「切り替えの超過回数」（④'）を入れる。scoreBetter でも増やさない。
- 「なるべく」でも、切り替えの超過は 🟡 に数えられ、4通りから選ぶ所と仕上げに
  使われる。**ほかの月では生成結果が変わることがある**（🚨は変わらず、🟡の中身が
  入れ替わる）。oct・oct11・oct16・nofix の各3回では、生成結果は前の版とすべて同じ
  だった。利用者はこれを了承したうえで入れた。
- **切り替えは今の決まりでは減らせない。** oct16 をじっくり（gap=0）で解くと、
  切り替えの段の最小は13件で、速い生成の14件とほぼ同じ。人員・公休・連勤・長い休み
  などの先の段を守ると、どうしても出る回数である。
- 超過した人の切り替えのうち、主担当が休んだ日の代わりは oct 32回中14回、
  oct16 25回中10回。早総務・遅総務だけでなく、責任者や早番の代わりもある。
- **「絶対」は勧めない。** oct16 で「絶対」にすると 🚨 0→5（連休が長すぎる2・
  切り替え3）で、上限を超える人は3人のまま、1人最多も5回のまま。
- 効かなかった案（設定だけを変えて各3回、🚨はどれも増えず）:
  - 案1 早総務の主担当の休みを「なるべく連休」: 切り替え oct 32→34〜39、oct16 25→27〜29。
  - 案2 主担当4人の休みを「なるべく連休」: 横ばい〜増、🟡 oct 16→30。
  - 案3 早番の主担当にも総務をできるようにする: oct の 🚨 3→2 だが切り替えは増えた。
  - 休み方は切り替えより後の段で解くので、切り替えを減らす方向には効かない。
- 早遅バランスと切り替えを両方「絶対」にすると、設定画面で知らせる（上の段落）。

結論として全体に効く一律の設定は無い。「早遅バランス＝均等」は個人設定
なので、遅番が偏っている人にだけ付けて、月ごとに測って決めること。

## 測り方（PDCA）

**生成結果は1回ごとに大きくぶれる。** 同じデータ・同じコードでも、4通りの
解き方の結果が 10件〜25件 のように散る。持ち時間が壁時計の秒数で決まって
いるため、CPU の混み具合で結果が変わる。1データ1回の比較では、変更の
効果と偶然の区別がつかない。

- **アプリと同じ条件で測る。** ブラウザは1つのデータの4通りを同時に解く
  （Worker 4本）。測るときも4通りを同時に走らせ、データは1つずつ順に。
  データを並べて同時に解くと条件が変わり、別の結果になる。
- **4通りの結果をすべて保存する。** 選び方の変更や、生成後の処理の変更は、
  保存した結果に後からかけるだけで比べられる（ソルバーを回し直さない）。
- **実データだけで決めない。** 実データの希望休の位置を入れ替えたもの、
  11月（30日）・12月（曜日のずれ）にずらしたものも作って確かめる。
  10月のデータだけに合わせた変更になっていないかを見るため。
- **下限（生成前チェックの最低エラー数）と比べる。** 下限に届いている月は
  それ以上減らせない。届いていない月にだけ改善の余地がある。
- **各データ3回以上。** 採否の決まりを測る前に決めておく。例: 5回の平均の差が、
  同じ条件の回ごとのばらつきより小さければ「効果なし」として外す。
- **実データは「10月の表の版違い」である。** oct・oct11〜oct16 は同じ10月の
  表の別の版で、7か月分ではない。ほかの月を見るには、作ったデータ
  （希望休の入れ替え s1/s2、11月 nov、12月 dec）を使う。
- **固定なし・人手不足のデータ（nofix）も入れる。** 固定セルが無いときだけ
  出る不具合がある（コンプラの段の答えが捨てられていた）。

採用したもの（10月の表の版違い7つ＋作ったデータ12個、🚨 77件 → 41件）:

- 4通りから選ぶときは 🚨 を先に比べる（milp.v6.js）。合計だけで比べて
  いたため、🚨が多いほうを採ることがあった。今は上の「並べ方」①〜⑤を使う。
- 生成後に polishShifts で、検査で確かめながら入れ替えで減らす。
  同日の出勤どうしの入れ替えと、たすき掛け（Aが d1=X・d2=Y、Bが d1=Y・d2=X）
  だけを使う。どちらも各人の出勤・公休・有給・余の日数と、各日の役割ごとの
  人数を変えない。出勤と休みを入れ替える手は、有給を別の人へ移してしまうので使わない。
- 利用者が「絶対」にしたルールを、🟡のルールより先の段で解く
  （promoteMustTiers）。段の並びは種類で固定されていたため、「絶対」の
  長い休み・早遅バランスが、🟡の早遅の切替より後に解かれていた。

6連勤以上を違反にした版（v217）の最終測定（2026年9月24日、20データ×各3回、
前の版 v216 と比べる。1回あたりの平均）:

| | ⛔ 6連勤以上 | 人員不足 | 🚨（⛔を除く） | 🟡 |
|---|---|---|---|---|
| v216 | 7 | 7 | 50 | 160.7 |
| v217 | **0** | 17 | 60 | 160.7 |

- 人員不足と 🚨 が増えたのは、⛔ を消した代わり。増えたのは ⛔ があった
  データ（nofix・oct_s2・oct13_s2）だけ。v216 の nofix は 26連勤の人がいた。
- nofix の人員不足13は、じっくり生成で「これ以上良い組み合わせは無い」と
  確認済み（6連勤以上を作らない条件での最小）。止めどころを外したことで
  人員不足が増えたのではない。
- 🟡 の合計は同じだが、データごとには動く（oct16 +3、oct15 −3 など）。

取り消したもの:

- 🚨の段が残ったら仕上げの時間を回して詰め直す: 🚨はどの月も同じ（84/🚨10
  と 82/🚨10）。後ろの段の上限がすでに固定されていて動かす余地がない。
- 段ごとの止めどころ（差が小さければ早めに次の段へ）: 5回測って nofix の
  人員不足が あり12.2・なし13.0。差0.8 は回ごとのばらつき（1）より小さく、効果なし。
- 仕上げに切り替えの上限を足す: 結果がすべて同じで効果なし。切り替えは
  生成の段と4通りの選び方で決まり、仕上げの入れ替えでは動かない。

検査・チェックの抜け（生成結果の良し悪しとは別に直したもの）:

- checkViolations が有給の日数を見ていなかった（'paid' を追加）。
- 生成前チェックが「必ずエラーになる」理由の多く（副店長不在、希望どうしの
  矛盾など）を最低エラー数に数えていなかった。「なし」にしたルールの理由は
  数えず、表示もしない。
- 生成前チェックが、余で休める月でも連勤を「避けられない」と言っていた。
- 21人以上の部門で「確認済み」が誤って出ていた。21人以上は「差2%・2000点以内なら
  止める」設定で解くので、HiGHS の Optimal は証明にならない（答えに gap は入って
  いない）。証明済みと数えるのは、gap=0 の設定で Optimal だった段と、件数0の段だけ
  （milp.worker.js の provenOf）。微調整（近くだけを探す）の Optimal も証明に数えない。

## 利用者のデータを守る（v221、2026年9月24日）

- **試し計算は本物の表を書き換えない。** 比べるだけ・候補を拾うだけの計算は
  `optimizeScheduleMILP(..., { noApply: true, settingsPatch })` で解く。設定を一時的に
  書き換えて戻す方式は、計算中の自動保存で変えた値が保存されるので使わない。
  4通りの途中の答えも画面へ反映しない（選び終わってから `_milpApply`）。
- **計算は一度に1つだけ。** 計算を始める所は `calcBegin(名前)` で始め、`finally` で
  `calcEnd()`。計算中は関係するボタンを押せなくし、開いておいた画面から押されたら
  知らせて始めない。表や固定を変えてから計算する処理は、変える前に確かめる。
- **中止は `cancelMILP()`。** 動いている数理最適化の Worker をすべて止め、結果を反映しない。
  止めたときに、計算の前に変えた固定・必要人数も元に戻す。
- **スタッフIDは `newStaffId()`。** いまいる人と、希望・固定・表に残っているIDの最大値
  より大きい番号にする（取り込んだファイルには通し番号が入っていない）。
- **取り込み・読込・リセットのあとは `resetShiftHistory()`。** 前のデータの表へ戻せないように。
- 前月末の引き継ぎの確認は、ちょうど翌月に進めたときだけ出す。

## 個人情報を公開リポジトリに入れない
このリポジトリは誰でも見られる。スタッフの名前は、コード・コメント・コミットの説明文・CLAUDE.md・README・manual.html・テスト用データのどこにも書かない。
- 例や説明では「Aさん」「Bさん」を使う（利用者へのチャットでの報告には名前を使ってよい）
- 実データ（アプリから書き出したファイルなど）はリポジトリに入れない。測定用のデータや手順を残すときは、名前を A〜 に置き換えたものだけにする
- push の前に、今回の変更とコミットの説明文に人の名前が入っていないか確認する
